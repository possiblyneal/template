# This repository builds other repositories; it is not a Python package and
# ships no Python of its own. The file exists because apps/repo-builder/ holds
# Python that scripts/check could not see: language detection reads a root
# manifest (libs/detect.sh has_python), and without one the gate reported there
# was nothing to check while a pytest suite sat untested. Declaring the language
# is what puts ruff, ty, and pytest over that code.
#
# Root-only, deliberately. A generated repository has no repo-builder unit, so
# the payload at apps/github-repository-template/src/base-repo/ must not carry
# this file -- it would declare Python in every repository that has none.

[project]
name = "template"
version = "0"
requires-python = ">=3.11"

[tool.uv]
# Nothing here is installable, so uv resolves the dev group into a virtualenv
# and stops. Without this uv looks for a build backend and fails on a repository
# that has no package to build.
package = false

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.14", "ty>=0.0.1a21"]

[tool.ruff.format]
# `*.md` is in ruff's own default include list, and `ruff format` rewrites the
# Python fenced inside one -- 44 of the 49 files it counted here were Markdown.
# Declining that is a boundary, not a preference: `scripts/fix` runs
# `ruff format .`, and Markdown is where this repository keeps two trees no tool
# may rewrite unasked -- the payload under
# apps/github-repository-template/src/base-repo/, which is data rather than this
# repository's configuration and whose edits are supposed to prompt, and
# docs/plans/, where an approved plan is frozen. Nothing diffs today because all
# 49 are already formatted, which is why it would land on someone later.
#
# Scoped to the formatter, and by file type rather than by path, so it neither
# takes Python out of scope -- the mistake the `include` here used to make --
# nor forecloses `ruff check` reaching a Markdown fence if it ever does.
exclude = ["*.md"]

[tool.pytest.ini_options]
# pytest is the only one of the three that needs telling where the code is, and
# the reason is the payload rather than the path: apps/github-repository-template/
# src/base-repo/ is data, and a .py added there is a generated repository's file,
# not a test of this one. Bounding collection is what keeps it from being
# collected as though it were. ruff and ty get no `include` here -- one would not
# widen what they see, it would narrow it to this path and silently drop any
# Python later added under apps/ or libs/.
testpaths = ["apps/repo-builder/src/scripts/tests"]
